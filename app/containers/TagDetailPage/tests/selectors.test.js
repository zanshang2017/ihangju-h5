import {
  selectTagDetailPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectTagDetailPage();

describe('selectTagDetailPage', () => {

});
